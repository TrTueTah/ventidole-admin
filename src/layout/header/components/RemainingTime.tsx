// import useRemainingTime from '@/hooks/useRemainingTime';

// function RemainingTime() {
//   const { minutes, seconds } = useRemainingTime();
//   const { t: tText } = useTranslation('text');

//   return (
//     <p className='my-auto text-sm font-medium text-ventidole-neutral-60 w-fit md:text-base'>
//       Remaining Time: {minutes.toString().padStart(2, '0')} {tText('minutes')} {seconds.toString().padStart(2, '0')}{' '}
//       {tText('seconds')}
//     </p>
//   );
// }

// export default RemainingTime;
